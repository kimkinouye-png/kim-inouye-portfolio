[build]
  # This is a static site, no build command needed
    publish = "."

    # Redirect all traffic to www.aperi.co
    [[redirects]]
      from = "/*"
        to = "https://www.aperi.co/:splat"
          status = 301
            force = true
